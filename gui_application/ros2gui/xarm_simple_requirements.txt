# xArm Simple Control Requirements
# Install with: pip install -r xarm_simple_requirements.txt

# Core xArm SDK - Direct TCP/IP communication (No ROS2 required)
xarm-python-sdk>=1.16.0

# GUI dependencies (should already be available)
PyQt5>=5.15.0

# Optional: For advanced features
numpy>=1.20.0
